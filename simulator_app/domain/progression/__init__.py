"""Map-matching geometry primitives for the databus simulator.

Translated from databus/backend/runs/domain/progression/ — kept independent
so the simulator has no runtime dependency on the databus repo.
"""
