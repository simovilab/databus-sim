"""API layer — DRF views, serializers, and URL patterns.

Submodules:
  views.py       — DRF ViewSet / APIView implementations (Phase 5)
  serializers.py — DRF serializers mirroring sim/http_control.py pydantic models (Phase 5)
  urls.py        — urlpatterns mounted at /sim/ (Phase 1 has GET /sim/healthz stub)
"""
