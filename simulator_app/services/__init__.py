"""Services layer — I/O-bound adapters (HTTP, MQTT scheduling).

Submodules (ported from sim/ in Phase 2):
  databus_client.py — DatabusClient (verbatim from sim/databus_client.py)
  run_binder.py     — RunBinder.poll_loop (verbatim from sim/run_binder.py)
  scheduler.py      — Scheduler.run_loop (verbatim from sim/scheduler.py)
"""
