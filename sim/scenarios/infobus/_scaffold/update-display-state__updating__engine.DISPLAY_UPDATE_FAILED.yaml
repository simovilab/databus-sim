id: update-display-state--updating--engine.DISPLAY_UPDATE_FAILED--stub
fsm: update-display-state
description: 'TODO: fill in description'
stub: true

expected_branch:
  terminal_state: notifying
  via_states: [updating]
  via_events: [engine.DISPLAY_UPDATE_FAILED]

# TODO: add inputs that drive the FSM into this failure branch
inputs: []

timeout_ms: 10000
tags: [update-display-state, failure-path]
