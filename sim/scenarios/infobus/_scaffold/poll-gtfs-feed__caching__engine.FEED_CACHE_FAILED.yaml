id: poll-gtfs-feed--caching--engine.FEED_CACHE_FAILED--stub
fsm: poll-gtfs-feed
description: 'TODO: fill in description'
stub: true

expected_branch:
  terminal_state: notifying
  via_states: [caching]
  via_events: [engine.FEED_CACHE_FAILED]

# TODO: add inputs that drive the FSM into this failure branch
inputs: []

timeout_ms: 10000
tags: [poll-gtfs-feed, failure-path]
