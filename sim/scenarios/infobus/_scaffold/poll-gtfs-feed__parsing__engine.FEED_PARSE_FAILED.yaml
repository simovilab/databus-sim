id: poll-gtfs-feed--parsing--engine.FEED_PARSE_FAILED--stub
fsm: poll-gtfs-feed
description: 'TODO: fill in description'
stub: true

expected_branch:
  terminal_state: notifying
  via_states: [parsing]
  via_events: [engine.FEED_PARSE_FAILED]

# TODO: add inputs that drive the FSM into this failure branch
inputs: []

timeout_ms: 10000
tags: [poll-gtfs-feed, failure-path]
