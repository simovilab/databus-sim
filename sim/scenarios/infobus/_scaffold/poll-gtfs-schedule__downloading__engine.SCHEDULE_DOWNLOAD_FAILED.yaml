id: poll-gtfs-schedule--downloading--engine.SCHEDULE_DOWNLOAD_FAILED--stub
fsm: poll-gtfs-schedule
description: 'TODO: fill in description'
stub: true

expected_branch:
  terminal_state: notifying
  via_states: [downloading]
  via_events: [engine.SCHEDULE_DOWNLOAD_FAILED]

# TODO: add inputs that drive the FSM into this failure branch
inputs: []

timeout_ms: 10000
tags: [poll-gtfs-schedule, failure-path]
