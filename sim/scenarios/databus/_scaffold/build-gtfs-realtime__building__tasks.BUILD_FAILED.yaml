id: build-gtfs-realtime--building--tasks.BUILD_FAILED--stub
fsm: build-gtfs-realtime
description: 'TODO: fill in description'
stub: true

expected_branch:
  terminal_state: notifying
  via_states: [building]
  via_events: [tasks.BUILD_FAILED]

# TODO: add inputs that drive the FSM into this failure branch
inputs: []

timeout_ms: 10000
tags: [build-gtfs-realtime, failure-path]
