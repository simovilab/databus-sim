id: register-run--registering--realtime_engine.RUN_INITIALIZATION_FAILED--stub
fsm: register-run
description: 'TODO: fill in description'
stub: true

expected_branch:
  terminal_state: notifying
  via_states: [registering]
  via_events: [realtime_engine.RUN_INITIALIZATION_FAILED]

# TODO: add inputs that drive the FSM into this failure branch
inputs: []

timeout_ms: 10000
tags: [register-run, failure-path]
