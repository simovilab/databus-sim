id: save-gtfs-feed-messages--persisting--tasks.PERSIST_FAILED--stub
fsm: save-gtfs-feed-messages
description: 'TODO: fill in description'
stub: true

expected_branch:
  terminal_state: notifying
  via_states: [persisting]
  via_events: [tasks.PERSIST_FAILED]

# TODO: add inputs that drive the FSM into this failure branch
inputs: []

timeout_ms: 10000
tags: [save-gtfs-feed-messages, failure-path]
