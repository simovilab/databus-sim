id: register-run--confirming--realtime_engine.RUN_CONFIRMATION_FAILED--stub
fsm: register-run
description: 'TODO: fill in description'
stub: true

expected_branch:
  terminal_state: notifying
  via_states: [confirming]
  via_events: [realtime_engine.RUN_CONFIRMATION_FAILED]

# TODO: add inputs that drive the FSM into this failure branch
inputs: []

timeout_ms: 10000
tags: [register-run, failure-path]
